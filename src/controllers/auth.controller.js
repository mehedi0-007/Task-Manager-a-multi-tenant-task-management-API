import {
  generateAccessToken,
  generateRefreshToken,
  verifyAccessToken,
  verifyRefreshToken,
} from "../config/jwt.js";

import user from "../models/user.models.js";

export const login = async (req, res) => {
    const 
};
